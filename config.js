exports.settings = {
    // Base URL for the web site that shows day care & child care inspection data.
    url_base : 'https://hs.ocfs.ny.gov/DCFS/Profile/Index/',

    // Port for server to listen on
    port: 3000
};